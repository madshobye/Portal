#pragma once

// Including this header makes Arduino compile the local ESP-IDF USB host
// source override in this library. The actual API remains ESP-IDF's
// usb/usb_host.h from the Arduino ESP32 core.
