import { createProjectShellRegistry } from "./project-shell-registry.js?v=0.1.87-ui720";
import { timestampForFilename } from "./download-utils.js?v=0.1.87-ui720";

export function createProjectFeatureRegistry({
  activeRevision,
  autoProjectName,
  buildRevision,
  codeHashFor,
  createProjectId,
  createRevisionId,
  documentRef,
  formatBytes,
  getChatMessages,
  getChatShellController,
  getCircuitShellController,
  getCircuitView,
  getCodeEditorShellController,
  getCommandConsoleService,
  getConsoleController,
  getCurrentProjectCircuit,
  getCurrentProjectDescription,
  getCurrentProjectDescriptionSource,
  getCurrentProjectId,
  getCurrentProjectSpecificationMode,
  getCurrentProjectSpecificationModeSource,
  getCurrentRevisionId,
  getCurrentSketchName,
  getCurrentSketchSaved,
  getCurrentSketchSource,
  getDeviceStateController,
  getLegacyProjectMigrationService,
  getProjectCache,
  getProjectDedupeService,
  getProjectSelectTimer,
  getProjectSchemaMigrationService,
  getProjectToolbarController,
  getRevisionNameDialog,
  getRevisionSelectTimer,
  getSpecificationEditorController,
  getStartupStepRunner,
  getStorageDiagnostics,
  getTransportClient,
  getWindow,
  hasCircuitChatLayout,
  legacySketchMigrationId,
  legacySketchMigrationVersion,
  localStorageRef,
  nextNamedRevisionName,
  nextRevisionName,
  normalizeChatMessages,
  normalizeCircuitLayout,
  normalizeProject,
  normalizeProjectName,
  normalizeProjectRecord,
  normalizeSketchName,
  normalizeSpecificationMode,
  projectCircuitForCurrentCode,
  projectFromCode,
  projectLimit,
  projectStoreName,
  projectWithRequiredRevision,
  readSpecificationMarkdown,
  revisionDraftVersion,
  revisionNameRoot,
  setChatMessages,
  setCircuitChatLayout,
  setCurrentProjectCircuit,
  setCurrentProjectDescription,
  setCurrentProjectDescriptionSource,
  setCurrentProjectId,
  setCurrentProjectSpecificationMode,
  setCurrentProjectSpecificationModeSource,
  setCurrentRevisionId,
  setCurrentSketchDirty,
  setCurrentSketchName,
  setCurrentSketchSaved,
  setCurrentSketchSource,
  setCurrentSketchVersionName,
  setProjectCache,
  setProjectSelectTimer,
  setRevisionSelectTimer,
  setSketchHistoryTitle,
  sketchDbName,
  sketchDbVersion,
  sketchStoreName,
  splitRevisionNumber,
  storage,
  URLRef,
} = {}) {
  let projectShellRegistry = null;

  function getProjectShellRegistry() {
    if (projectShellRegistry) return projectShellRegistry;
    projectShellRegistry = createProjectShellRegistry({
      activeRevision,
      autoProjectName,
      buildRevision,
      codeHashFor,
      createProjectId,
      createRevisionId,
      documentRef,
      formatBytes,
      getChatMessages,
      getChatShellController,
      getCircuitShellController,
      getCircuitView,
      getCodeEditorShellController,
      getCommandConsoleService,
      getConsoleController,
      getCurrentProjectCircuit,
      getCurrentProjectDescription,
      getCurrentProjectDescriptionSource,
      getCurrentProjectId,
      getCurrentProjectSpecificationMode,
      getCurrentProjectSpecificationModeSource,
      getCurrentRevisionId,
      getCurrentSketchName,
      getCurrentSketchSaved,
      getCurrentSketchSource,
      getDeviceStateController,
      getLegacyProjectMigrationService,
      getProjectCache,
      getProjectDedupeService,
      getProjectSelectTimer,
      getProjectSchemaMigrationService,
      getProjectToolbarController,
      getRevisionNameDialog,
      getRevisionSelectTimer,
      getSpecificationEditorController,
      getStartupStepRunner,
      getStorageDiagnostics,
      getTransportClient,
      getWindow,
      hasCircuitChatLayout,
      legacySketchMigrationId,
      legacySketchMigrationVersion,
      localStorageRef,
      nextNamedRevisionName,
      nextRevisionName,
      normalizeChatMessages,
      normalizeCircuitLayout,
      normalizeProject,
      normalizeProjectName,
      normalizeProjectRecord,
      normalizeSketchName,
      normalizeSpecificationMode,
      projectCircuitForCurrentCode,
      projectFromCode,
      projectLimit,
      projectStoreName,
      projectWithRequiredRevision,
      readSpecificationMarkdown,
      revisionDraftVersion,
      revisionNameRoot,
      setChatMessages,
      setCircuitChatLayout,
      setCurrentProjectCircuit,
      setCurrentProjectDescription,
      setCurrentProjectDescriptionSource,
      setCurrentProjectId,
      setCurrentProjectSpecificationMode,
      setCurrentProjectSpecificationModeSource,
      setCurrentRevisionId,
      setCurrentSketchDirty,
      setCurrentSketchName,
      setCurrentSketchSaved,
      setCurrentSketchSource,
      setCurrentSketchVersionName,
      setSketchHistoryTitle,
      setProjectCache,
      setProjectSelectTimer,
      setRevisionSelectTimer,
      sketchDbName,
      sketchDbVersion,
      sketchStoreName,
      splitRevisionNumber,
      storage,
      timestampForFilename,
      URLRef,
    });
    return projectShellRegistry;
  }

  return {
    getCurrentRevisionSession: () => getProjectShellRegistry().getCurrentRevisionSession(),
    getProjectActionsController: () => getProjectShellRegistry().getProjectActionsController(),
    getProjectController: () => getProjectShellRegistry().getProjectController(),
    getProjectDownloadService: () => getProjectShellRegistry().getProjectDownloadService(),
    getProjectHistoryView: () => getProjectShellRegistry().getProjectHistoryView(),
    getProjectImporter: () => getProjectShellRegistry().getProjectImporter(),
    getProjectLibraryService: () => getProjectShellRegistry().getProjectLibraryService(),
    getProjectRevisionService: () => getProjectShellRegistry().getProjectRevisionService(),
    getProjectShellRegistry,
    getProjectStore: () => getProjectShellRegistry().getProjectStore(),
    getRevisionDraftStore: () => getProjectShellRegistry().getRevisionDraftStore(),
  };
}
